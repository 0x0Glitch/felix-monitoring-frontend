# Database Configuration
NEXT_PUBLIC_DATABASE_URL=your-database-url-here

# Default Database Schemas
# These are used as fallback schemas when not specified in market configuration
NEXT_PUBLIC_MARKET_SCHEMA=market_data
NEXT_PUBLIC_USER_POSITIONS_SCHEMA=user_positions

# Legacy Configuration (kept for backward compatibility)
# These are now configured per-market in lib/markets.ts
NEXT_PUBLIC_DEFAULT_MARKET=flx:TSLA
NEXT_PUBLIC_MARKET_TABLE=flx_tsla_data
NEXT_PUBLIC_USER_POSITIONS_TABLE=flx_tsla_positions

# ============================================
# MULTI-MARKET CONFIGURATION
# ============================================
# Markets are now configured in lib/markets.ts
# Each market can have its own:
# - Database schemas
# - Table names
# - Display settings
#
# Table Naming Convention:
# - Market data: {dex}_{symbol_lowercase}_data
# - User positions: {dex}_{symbol_lowercase}_positions
#
# Example markets:
# - flx:TSLA → Tables: flx_tsla_data, flx_tsla_positions
# - xyz:XYZ100 → Tables: xyz_xyz100_data, xyz_xyz100_positions
# - flx:AAPL → Tables: flx_aapl_data, flx_aapl_positions
